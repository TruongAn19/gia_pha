import AppShell from './components/AppShell'

// Bước 2 (nền UI): chỉ khung chung (Sidebar + nội dung + BottomTabBar).
// Các màn nội dung sẽ dựng ở những bước sau (DESIGN_NOTES §5).
export default function App() {
  return <AppShell />
}
